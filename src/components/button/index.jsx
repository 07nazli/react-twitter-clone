/* import { createElement } from "react";
import classNames from "classnames";

export default function Button({ size, children }) {
    return createElement('button', {
        className: classNames("bg-[#1d9bf0] rounden-full", {
            "px-4": size === 'normal'
        })
    }, children)
}

Button.propTypes = {
    size: PropType.oneOf(['normal', 'large'])
}

Button.defaultProps = {
    size: 'normal'
}
*/